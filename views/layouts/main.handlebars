<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Social Strength</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header>
    <h1>Welcome to Social Strength</h1>
  </header>
  <nav>
    <a href="/">Home</a>
    {{#if loggedIn}}
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
  </nav>
  <main>
    <aside>
      
      
    </aside>
    <div>
      {{{body}}}
    </div>
  </main>
  <footer>
    <h2>
      {{#if loggedIn}}

      <div></div>
      {{else}}
      <div>you are logged out</div>
{{/if}}
    </h2>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
